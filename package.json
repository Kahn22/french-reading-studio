{
  "name": "french-reading-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Content foundation for learning French through public-domain literature.",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:domain && npm run build:app",
    "build:domain": "tsc -p tsconfig.build.json",
    "build:app": "vite build",
    "check": "npm run typecheck && npm test",
    "content:prepare": "npm run build && node dist/cli/prepare-fixtures.js",
    "content:build": "npm run build && node dist/cli/prepare-fixtures.js && node dist/cli/build-corbeau.js",
    "content:check": "npm run content:build && git diff --exit-code -- content",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --dir tests"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "vitest": "^3.2.4"
  }
}
