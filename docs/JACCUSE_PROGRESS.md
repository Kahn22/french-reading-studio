# J’Accuse production checkpoint

The local `Build production foundation` workspace is the current source of this checkpoint. Regenerate the review and audit with `npm run pipeline:zola`; do not infer readiness from candidate coverage alone.

- Canonical source: 199 thought units; 4,889 token candidates.
- Lexical review: 4,889 decisions, zero unresolved candidates.
- Planned expressions: 11 occurrences across 10 identities; all 10 have three fixed prepared quiz bands. The two occurrences of `de sorte que` reuse one expression identity and question set.
- Vocabulary review output: 3,876 quiz items across 1,411 required surface-and-sense identities. The reproducible `content/review/wrk_zola_jaccuse.quiz-coverage.json` report identifies 119 sets fully reusable from the global bank and 1,292 clean work-prepared sets. Zero identities require editorial revision, have partial global coverage, or lack a clean quiz band.
- The originally remaining 1,020 identities are frozen into two disjoint batches of 510 in `content/review/wrk_zola_jaccuse.editorial-batch-{1,2}.json`. `content/review/wrk_zola_jaccuse.editorial-progress.json` now reports both batches at 510 prepared and zero needing revision.
- The assembled content has no remaining schema, reference, answer-choice, quiz-context, or expression-validation errors. The dedicated verbatim audit reports zero source-reused questions, zero source-derived blanks, zero synthetic choice lists, and zero identities requiring question review.
- Quiz-rule decision resolved: the user explicitly confirmed the current three-context implementation over the older seven-context minimum in `French_Reading_Studio_Quiz_Rules.txt`. Each identity requires one prepared question for Levels 1–3, one shared by Levels 4–5, and one for Levels 6–8. Preserve all other quiz-quality rules.
- Review decision resolved: the user explicitly waived a further human quiz review and requires only the deterministic verbatim source-reuse checks before publication.
- Local publication: `published`. `content/learning/jaccuse.json` is the materialized learner bundle and `content/learning/jaccuse-expressions.json` is its delivered expression catalog. The learner app exposes J’Accuse alongside the two La Fontaine works; vocabulary and expressions retain separate global mastery identities.
- Authoring and deferred validation are complete. The final group covers the 164 identities that remained in editorial batch 2; seven additional tail specifications already covered by the global quiz bank were also given static editorial definitions, but are not needed for J’Accuse coverage. The large groups use static, independently generated pedagogical contexts derived from each reviewed sense; they do not reuse J’Accuse source sentences and introduce no runtime generation. The complete pipeline, dedicated audit, 143-test suite, production build, and diff check pass.
- Final verification now passes 143 tests. The delivery build writes 226 reading sections and 154 vocabulary quiz batches, while the manifest carries all 10 expression identities and 30 prepared expression questions.
- Local release smoke QA loads the production app shell, J’Accuse’s first incremental reading section, and a vocabulary quiz payload successfully. Expression taps now open the same kind of mastery, meaning, schedule, and review sheet as vocabulary taps, and the global review copy explicitly includes both words and expressions. Responsive rules cover a single-column layout, safe-area insets, and the 320-pixel supported minimum width.
- The learner home now uses three non-monetary reading-library states: Available, Reading, and Completed. Any number of texts may be Reading. Only Reading texts open in the reader; Reading and Completed texts both contribute vocabulary/expression quizzes. Moving a text to Available preserves mastery and due timestamps while deactivating its unique quizzes, and “Read again” restores reading access for a completed text.

No external publication action has been taken: these changes remain local and have not been pushed to GitHub or deployed. The next boundary is hands-on Safari/iPhone learner-flow QA, followed by a separately authorized push and deployment.
